# Local Postgres overrides for backend development
# Copy to .env.postgres.local (ignored) and adjust host/port as needed.

DB_PROVIDER=postgres
DATABASE_URL=postgres://classwaves:classwaves@localhost:5433/classwaves_dev
DB_SSL=0
DB_POOL_MIN=1
DB_POOL_MAX=10
DB_LOG_SQL=0

# Optional verbose SQL logging (default 0)
# DB_LOG_SQL=1

# If docker host port differs, update DATABASE_URL accordingly, e.g.:
# DATABASE_URL=postgres://classwaves:classwaves@localhost:5435/classwaves_dev
